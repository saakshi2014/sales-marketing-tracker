services:
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: smt_backend
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    env_file:
      - .env
    restart: unless-stopped